<div id="{{id}}" class="item">
  <div class="content">
    <div class="header">
      <strong>{{sender_name}}:</strong> {{subject}}
      {{#unless readed}}
      <button class="ui topright icon basic tiny button readed-btn" data-content="Пометить как прочитанное." data-position="left center" data-variation="inverted">
        <i class="close icon"></i>
      </button>
      {{/unless}}
    </div>
    <div class="description">
      {{{markdown body}}}
    </div>
  </div>
</div>
